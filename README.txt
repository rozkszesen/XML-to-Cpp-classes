This program generates c++ classes from input XML file. 
The output should be separate header files with class definitions and main.cpp file with declarations of instances of classes.

To run the program:
./python class_generator.py input_file_name.xml